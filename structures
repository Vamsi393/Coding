Q)Create a structure to represent a student with the following members: name (string), roll number
(int), and marks (float). Write a function to display the details of a student. 
A)#include<stdio.h>
struct student
{
    int rollno;
    char name[20];
    float marks;
};
void main()
{
    struct student s1={2,"vamsi",98.7};
    printf("%d %s %f",s1.rollno,s1.name,s1.marks);
}
OUTPUT:2 vamsi 98.699997

ANOTHER MODEL FOR more students data using array
#include<stdio.h>
struct student
{
    int rollno;
    char name[20];
    float marks;
};
void main()
{
    struct student s1[8];
    for(int i=0;i<8;i++)
    {
        scanf("%d %s %f",&s1[i].rollno,s1[i].name,&s1[i].marks);
    }
    for(int i=0;i<8;i++)
    {
    printf("%d %s %.2f",s1[i].rollno,s1[i].name,s1[i].marks);
    }
}
Q)Define a union to store either an integer or a floatingpoint number. Write a function to accept the
type of data (integer or float) and then read the corresponding value from the user. Store the value in
the union and print it?
A)#include<stdio.h>
union number{
    int i;
    float f;
};
int vamsi(int);
int main()
{
    int choice;
    printf("enter a choice");
    scanf("%d",&choice);
    vamsi(choice);
}
int vamsi(int choice)
{
    union number n;
    if(choice==1)
    {
        printf("enter a integer");
        scanf("%d",&n.i);
        printf("you entered number is %d",n.i);
    }
    else if(choice==2)
    {
        printf("enter a float number");
        scanf("%f",&n.f);
        printf("you enetrd number is %f",n.f);
    }
}
OUTPUT:enter a choice2
enter a float number56.8
you enetrd number is 56.799999
enter a choice1
enter a integer87
you entered number is 87

Q)Define a structure to represent a point in 2D space with x and y coordinates (both integers). Write a
function to check if two points are equal (have the same x and y coordinates)?
A)#include<stdio.h>
struct point 
{
    int x;
    int y;
};
int isequal( struct point p1, struct point p2)
{
    if (p1.x==p2.x && p1.y==p2.y)
    {
        return 1;
    }
    else
    {
        return 0;
    }
    
}
int main()
{
    struct point p1,p2;
printf("enter the first point:(x,y)");
scanf("%d %d",&p1.x,&p1.y);
printf("enter the second point:(x,y)");
getchar();
scanf("%d %d", &p2.x, &p2.y);
if (isequal(p1,p2)) {
    printf("two points are equal");
}
else
{
    printf("two points are not equal");
}
return 0;
}
OUTPUT:
enter the first point:(x,y)1,2
enter the second point:(x,y)4,6
two points are not equal


