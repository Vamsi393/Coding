~~~
WRITE A PROGRAM TO CLEAR 4TH AND 2ND BIT OF NUMBER?
A)
#include<stdio.h>
int main()
{
    int num,pos1,pos2;
     printf("enter the number:");
    scanf("%d",&num);
    printf("enter the pos:");
    scanf("%d",&pos1);
    printf("enter the pos1:");
    scanf("%d",&pos2);
    num=num&~(1<<pos1);
    num=num&~(1<<pos2);
    //num=num&~(1<<3);
    //num=num&~(1<<1);
    printf("original number is %d\n",num);
    printf("after clearing is 4th and 2nd bit is %d",num);
}
OUTPUT:
enter the number:13
enter the pos:3
enter the pos1:1
original number is 5
after clearing is 4th and 2nd bit is 5
~~~

2Q)
WRITE A PROGRAM TO SET 5TH ,3rd AND 1st BIT OF NUMBER?
A)
#include<stdio.h>
int main()
{
    int num,pos1,pos2,pos3;
    printf("enter the number:");
    scanf("%d",&num);
    printf("enter the position1:");
    scanf("%d",&pos1);
    printf("enter the position2:");
   scanf("%d",&pos2);
   printf("enter the position3:");
   scanf("%d",&pos3);
    num=num|(1<<pos1);
    num=num|(1<<pos2);
    num=num|(1<<pos3);
    printf("%d",num);
}
OUTPUT:
enter the number:25
enter the position1:5
enter the position2:3
enter the position3:1
59

4Q)
WRITE A PROGRAM TO TOGGLE 7TH,5TH ,4TH AND 1st BIT OF NUMBER?
A)
#include<stdio.h>
int main()
{
    int num,pos1,pos2,pos3,pos4;
    printf("enter the number:");
    scanf("%d",&num);
    printf("enter the position1:");
    scanf("%d",&pos1);
    printf("enter the position2:");
   scanf("%d",&pos2);
   printf("enter the position3:");
   scanf("%d",&pos3);
   printf("enter the position4:");
   scanf("%d",&pos4);
    num=num^(1<<pos1);
    num=num^(1<<pos2);
    num=num^(1<<pos3);
    printf("%d",num);
}
OUTPUT:
enter the number:25
enter the position1:7
enter the position2:5
enter the position3:4
enter the position4:1
169

3)Q
WRITE A PROGRAM TO FIND THE STATE OF 3RD AND 2ND BIT OF NUMBER?
A)
#include<stdio.h>
int main()
{
    int num,pos1,pos2;
    printf("enter the number:");
    scanf("%d",&num);
    printf("enter the pos1:");
    scanf("%d",&pos1);
    printf("enter the pos2:");
    scanf("%d",&pos2);
    int bit1=num&(1<<pos1)?1:0;
    int bit2=num&(1<<pos2)?1:0;
    printf("bit at position %d=%d\n",pos1,bit1);
     printf("bit at position %d=%d\n",pos2,bit2);
}
OUTPUT:
enter the number:13
enter the pos1:2
enter the pos2:1
bit at position 2=1
bit at position 1=0

Q)WRITE A PROGRAM TO COUNT THE NUMBER OF SET BITS IN A NUMBER?
A)
#include<stdio.h>
int main()
{
    int num,count=0;
    printf("enter the number:");
    scanf("%d",&num);
    int temp=num;
    while(temp>0)
    {
        if(temp&1)//check last bit is 1
        {
            count++;
        }
        temp=temp>>1;
    }
    printf("no.of set bits in number is %d=%d",num,count);
}
OUTPUT:
enter the number:13
no.of set bits in number is 13=3

Q)WRITE A PROGRAM TO SWAP TWO NUMBERS WITHOUT USING THIRD VARIABLE?
A)
#include<stdio.h>
int main()
{
    int a,b;
    printf("enter the number a:");
    scanf("%d",&a);
    printf("enter the number b:");
    scanf("%d",&b);
    a=a^b;
    b=a^b;
    a=a^b;
    printf("after swapping a is :%d\n",a);
    printf("after swapping b is :%d",b);
}
OUTPUT:
enter the number a:4
enter the number b:3
after swapping a is :3
after swapping b is :4

Q)
WRITE A PROGRAM TO SWAP LOWER AND HIGHER NIBBELS OF NUMBER?
A)
#include<stdio.h>
int main()
{
    unsigned char num;
    printf("enter a number(0-255):");
    scanf("%hhu",&num);//hhx is used to because read the unsigned char
    unsigned char result=((num&0x0f)<<4)|((num&0xf0)>>4);
    printf("before swapping nibbles(original number):0x%02X\n",num);
    printf("after swapping nibbles:0x%02X\n",result);
}
OUTPUT:
enter a number(0-255):20
before swapping nibbles(original number):0x14
after swapping nibbles:0x41

