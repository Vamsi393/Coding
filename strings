##Write a C program to sort a string array in ascending order?
```#include<stdio.h>
#include<string.h>
int main()
{
    char s[10];
    int i,j;
    printf("enter the string");
    scanf("%s",s[i]);
    for(int i=0;s[i]!='\0';i++)
    {
        for(int j=i+1;s[j]!='\0';i++)
        {
            if (s[i]>s[j])
            {
                char a;
                 a=s[i];
                s[i]=s[j];
                s[j]=a;
                }
        }
        }      
        printf("string in ascending order is:");
        for (int i=0;s[i]!='\0';i++)
        {
            printf("%c",s[i]);
        }
        
}
,,,
##Write a C program to check whether a substring is present in a string?
,,,#include <stdio.h>
#include<string.h>
int main()
{
    char str[10];
    int i,j,k;
    int count=0;
    printf("enter the string");
    scanf("%s",str);
    int len=strlen(str);
    printf("all the substrings are %s:",str);
    for (i=0;i<len;i++)
    {
        for(j=i;j<len;j++)
        {
            for(k=i;k<=j;k++)
            {
                printf("%c",str[k]);
            }
            printf("\n");
        }
    }
    
    
}
output;
enter the string:hello
all the substrings are hello:h
he
hel
hell
hello
e
el
ell
ello
l
ll
llo
l
lo
o


    

