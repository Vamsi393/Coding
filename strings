##Write a C program to sort a string array in ascending order?
```#include<stdio.h>
#include<string.h>
int main()
{
    char s[10];
    int i,j;
    printf("enter the string");
    scanf("%s",s[i]);
    for(int i=0;s[i]!='\0';i++)
    {
        for(int j=i+1;s[j]!='\0';i++)
        {
            if (s[i]>s[j])
            {
                char a;
                 a=s[i];
                s[i]=s[j];
                s[j]=a;
                }
        }
        }      
        printf("string in ascending order is:");
        for (int i=0;s[i]!='\0';i++)
        {
            printf("%c",s[i]);
        }
        
}
,,,
##Write a C program to check whether a substring is present in a string?
,,,#include <stdio.h>
#include<string.h>
int main()
{
    char str[10];
    int i,j,k;
    int count=0;
    printf("enter the string");
    scanf("%s",str);
    int len=strlen(str);
    printf("all the substrings are %s:",str);
    for (i=0;i<len;i++)
    {
        for(j=i;j<len;j++)
        {
            for(k=i;k<=j;k++)
            {
                printf("%c",str[k]);
            }
            printf("\n");
        }
    }
    
    
}
output;
enter the string:hello
all the substrings are hello:h
he
hel
hell
hello
e
el
ell
ello
l
ll
llo
l
lo
o



#Write a program in C to read a sentence and replace lowercase characters with uppercase
and vice versa?
```#include<stdio.h>
#include<ctype.h>//coverting charcters
int main()
{
    char str[10];
    printf("enter the string");
    scanf("%s",str);
    for( int i=0;str[i]!='\0';i++)
    {
    if(islower (str[i]))
    {
    str[i]=toupper(str[i]);
    }//converting lowercase to uppercae
    else if(isupper(str[i]))
    {
    str[i]=tolower(str[i]);
    }//uppercase to lower case
}
printf("modified string is %s",str);
}
output:
enter the stringvamsi
modified string is VAMSI```


### Write a program in C to remove characters from a string except alphabets?
#include<stdio.h>
int main()
{
    char str[100];
    char result[100];
    int i,j=0;
    printf("enter the string");
    scanf("%s",str);
    for (i=0;str[i]!='\0';i++)
    {
        if((str[i]>='A' && str[i]<='Z') || (str[i]>='a' && str[i]<='z'))
        {
        result[j]=str[i];
        j++;
        }
    }
    result[j]='\0';
    printf("alphabets in the string is %s",result);
        
    }
ouput:
enter the stringvams143i
alphabets in the string is vamsi


##### Write a program in C to find the frequency of characters?
#include<stdio.h>
#include<string.h>
int main()
{
    char str[10]="banana";
    int i,j,count;
    int len=strlen(str);
    for(i=0;i<len;i++)
    {
       count=1;
        if (str[i]=='0')
        continue;
    for(j=i+1;j<len;j++)
    {
        if (str[i]==str[j])
        {
        count++;
        str[j]='0';
        }
        
    }
    printf("%c:%d\n",str[i],count);
}
}
output:
b:1
a:3
n:2

###Write a program in C to combine two strings manually?
#include<stdio.h>
#include<string.h>
int main()
{
    char str1[20];
    char str2[20];
    strcpy(str1,"rajana\t");
    strcpy(str2,"vamsikrishna");
    strcat(str1,str2);
    printf("%s",str1);
    
}
OUTPUT:rajana	vamsikrishna
another method:
#include<stdio.h>
#include<string.h>
int main()
{
    char str1[10];
    char str2[10];
    char combined[20];
    int i,j;
    printf("enter the string");
    scanf("%s",str1);
    printf("enter the string2");
    scanf("%s",str2);
    for (i=0;str1[i]!='\0';i++){
        combined[i]=str1[i];
    }
    for (j=0;str2[j]!='\0';j++)
    {
        combined[i+j]=str2[j];
    }
    printf("%s",combined);
}
OUTPUT:enter the stringvamsi
enter the string2krishn
vamsikrishn


Write a program in C to convert a string to uppercase?
#include<stdio.h>
#include<ctype.h>
int main()
{
    int i;
    char str[10]="vamsi";
    for(i=0;str[i]!='\0';i++)
    {
    str[i]=toupper (str[i]);
    }
    printf("%s",str);
}
OUTPUT:
VAMSI

Write a program in C to convert a string to lowercase?
#include<stdio.h>
#include<ctype.h>
int main()
{
    int i;
    char str[20];
    printf("enter the string");
    scanf("%s",str);
    for(i=0;str[i]!='\0';i++)
    {
        str[i]=tolower (str[i]);
    }
    printf("%s",str);
}
OUTPUT:enter the string:VAMSI
vamsi

Write a program in C to check whether a character is a Hexadecimal Digit or not?
#include<stdio.h>
#include<ctype.h>
int main()
{
    char ch;
    printf("enter the character");
    scanf("%c",&ch);
        if(ch>='a' && ch<='f' || ch>='A' && ch<='Z'){
            printf("hexa digits are found");
        }
        else
        {
        printf("not found");
        }
}
OUTPUT:
enter the character:a
hexa digits are found






    

